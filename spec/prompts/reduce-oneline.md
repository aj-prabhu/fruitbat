Below are one-line summaries of consecutive parts of one document. Combine them into one line that states the document's single most important point. Rules:
- One line only, at most 25 words, starting with "- ". Output nothing else.
- Use only what the lines say. Keep numbers and names exactly as written. Keep "not" and "never" where the lines have them.
- Prefer the point that most of the lines share.

Lines:
{{lines}}
