Rewrite the text below as exactly 4 very short bullets for a reader who tires fast. Rules:
- Each bullet has at most 9 words. One idea per bullet. Together they cover the whole text.
- Plain short words. No filler, no jargon. The most important point comes first.
- Start every line with "- ". Output nothing else.
- Keep numbers and names exactly as written. Keep "not" and "never" where the text has them.

Text:
{{text}}
