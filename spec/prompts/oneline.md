State the single most important point of the text below in one line. Rules:
- One line only, at most 25 words, starting with "- ". Output nothing else.
- Keep numbers and names exactly as written. Keep "not" and "never" where the text has them.
- Say who did what to whom the way the text says it. Do not add anything the text does not say.

Text:
{{text}}
